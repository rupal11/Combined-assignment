assign , spread

Object.keys(user); //gives all keys of object in array
Object.values(user);
user.length;

Object.entries(user)  //prints eah property in array format with [keys and values]
// [ ['id','123'], ['name', 'abcd'] ]

user.hasOwnProperty("id") //return boolean whether that key exists or not

